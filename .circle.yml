machine:
  node:
    version: 9.3.0

deployment:
  dev:
    branch: dev
    commands:
      - npm test
      # - ./deploy/dev/ci/deploy.sh

  production:
    branch: master
    commands:
      - npm test
      # - ./deploy/prod/ci/deploy.sh
